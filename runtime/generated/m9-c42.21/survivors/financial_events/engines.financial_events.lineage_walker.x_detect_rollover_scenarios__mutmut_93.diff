# status: survived
# engines.financial_events.lineage_walker.x_detect_rollover_scenarios__mutmut_93: survived
--- src/engines/financial_events/lineage_walker.py
+++ src/engines/financial_events/lineage_walker.py
@@ -54,7 +54,7 @@
             for source in potential_sources:
                 source_date = _parse_date_iso(source.get("date_iso", ""))
                 if source_date is None:
-                    continue
+                    break
 
                 days_diff = _date_difference_days(source.get("date_iso", ""), date_iso)
                 if 0 < days_diff <= lookback_days:
