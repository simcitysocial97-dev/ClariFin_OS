# status: survived
# engines.financial_events.lineage_walker.x_detect_revocations__mutmut_54: survived
--- src/engines/financial_events/lineage_walker.py
+++ src/engines/financial_events/lineage_walker.py
@@ -42,7 +42,7 @@
         event_type = event.get("event_type", "")
         event_id = event.get("id", 0)
         transfer_id = event.get("transfer_id", "")
-        date_iso = event.get("date_iso", "")
+        date_iso = event.get("date_iso", None)
         lifecycle_state = event.get("lifecycle_state", "")
 
         # Only process revocation events
