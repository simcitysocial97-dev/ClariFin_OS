# status: no tests
# engines.financial_events.lineage_walker.x__is_revocable_event__mutmut_13: no tests
--- src/engines/financial_events/lineage_walker.py
+++ src/engines/financial_events/lineage_walker.py
@@ -1,7 +1,7 @@
 def _is_revocable_event(event: dict[str, Any]) -> bool:
     """Check if event type can be revoked (transfers only)."""
     event_type = event.get("event_type", "")
-    lifecycle_state = event.get("lifecycle_state", "")
+    lifecycle_state = event.get("lifecycle_state", )
     return event_type in (
         "fund_transfer_out",
         "fund_transfer_in",
