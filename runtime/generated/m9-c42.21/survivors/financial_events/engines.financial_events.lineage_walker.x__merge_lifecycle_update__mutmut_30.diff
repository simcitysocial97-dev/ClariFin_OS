# status: survived
# engines.financial_events.lineage_walker.x__merge_lifecycle_update__mutmut_30: survived
--- src/engines/financial_events/lineage_walker.py
+++ src/engines/financial_events/lineage_walker.py
@@ -13,7 +13,7 @@
     candidate_rank = _LIFECYCLE_STATE_RANK.get(
         candidate.get("lifecycle_state", "open"), 0
     )
-    if candidate_rank > existing_rank:
+    if candidate_rank >= existing_rank:
         return candidate
     if candidate_rank < existing_rank:
         return existing
