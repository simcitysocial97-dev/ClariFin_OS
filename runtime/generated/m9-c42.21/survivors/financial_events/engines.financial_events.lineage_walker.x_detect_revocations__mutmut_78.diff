# status: survived
# engines.financial_events.lineage_walker.x_detect_revocations__mutmut_78: survived
--- src/engines/financial_events/lineage_walker.py
+++ src/engines/financial_events/lineage_walker.py
@@ -50,7 +50,7 @@
             continue
 
         # Find the transfer this revocation applies to
-        transfer_events = events_by_transfer.get(transfer_id, [])
+        transfer_events = events_by_transfer.get(transfer_id, None)
         if not transfer_events:
             continue
 
