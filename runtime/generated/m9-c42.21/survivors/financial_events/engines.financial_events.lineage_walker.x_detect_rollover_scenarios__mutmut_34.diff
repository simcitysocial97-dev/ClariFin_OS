# status: survived
# engines.financial_events.lineage_walker.x_detect_rollover_scenarios__mutmut_34: survived
--- src/engines/financial_events/lineage_walker.py
+++ src/engines/financial_events/lineage_walker.py
@@ -25,7 +25,7 @@
         event_type = event.get("event_type", "")
         event_id = event.get("id", 0)
         event.get("account_id", "")
-        date_iso = event.get("date_iso", "")
+        date_iso = event.get("date_iso", "XXXX")
 
         # Look for advance events that might be rollovers
         if event_type in (
