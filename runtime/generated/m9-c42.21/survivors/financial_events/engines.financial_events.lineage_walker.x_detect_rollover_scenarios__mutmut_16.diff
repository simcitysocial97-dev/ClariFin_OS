# status: survived
# engines.financial_events.lineage_walker.x_detect_rollover_scenarios__mutmut_16: survived
--- src/engines/financial_events/lineage_walker.py
+++ src/engines/financial_events/lineage_walker.py
@@ -23,7 +23,7 @@
 
     for event in events:
         event_type = event.get("event_type", "")
-        event_id = event.get("id", 0)
+        event_id = event.get("id", )
         event.get("account_id", "")
         date_iso = event.get("date_iso", "")
 
