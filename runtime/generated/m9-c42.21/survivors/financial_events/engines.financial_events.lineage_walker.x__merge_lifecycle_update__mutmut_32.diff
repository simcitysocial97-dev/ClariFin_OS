# status: survived
# engines.financial_events.lineage_walker.x__merge_lifecycle_update__mutmut_32: survived
--- src/engines/financial_events/lineage_walker.py
+++ src/engines/financial_events/lineage_walker.py
@@ -18,7 +18,7 @@
     if candidate_rank < existing_rank:
         return existing
     # Same state rank: prefer smaller outstanding balance
-    existing_out = int(existing.get("outstanding_paise", 0) or 0)
+    existing_out = None
     candidate_out = int(candidate.get("outstanding_paise", 0) or 0)
     if candidate_out < existing_out:
         return candidate
