# status: survived
# engines.financial_events.lineage_walker.x_detect_revocations__mutmut_155: survived
--- src/engines/financial_events/lineage_walker.py
+++ src/engines/financial_events/lineage_walker.py
@@ -97,7 +97,7 @@
 
     # Apply auto-revocations for failed transfers
     for transfer_id in auto_revoked_transfers:
-        transfer_events = events_by_transfer.get(transfer_id, [])
+        transfer_events = events_by_transfer.get(transfer_id, None)
         for transfer_event in transfer_events:
             transfer_event_id = transfer_event.get("id", 0)
             lifecycle_updates.append(
