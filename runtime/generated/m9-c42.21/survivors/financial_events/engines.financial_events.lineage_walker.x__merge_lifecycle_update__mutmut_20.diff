# status: survived
# engines.financial_events.lineage_walker.x__merge_lifecycle_update__mutmut_20: survived
--- src/engines/financial_events/lineage_walker.py
+++ src/engines/financial_events/lineage_walker.py
@@ -11,8 +11,7 @@
         existing.get("lifecycle_state", "open"), 0
     )
     candidate_rank = _LIFECYCLE_STATE_RANK.get(
-        candidate.get("lifecycle_state", "open"), 0
-    )
+        candidate.get("lifecycle_state", "open"), )
     if candidate_rank > existing_rank:
         return candidate
     if candidate_rank < existing_rank:
