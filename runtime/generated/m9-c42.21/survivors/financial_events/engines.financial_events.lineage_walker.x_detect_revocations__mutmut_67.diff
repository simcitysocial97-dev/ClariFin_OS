# status: survived
# engines.financial_events.lineage_walker.x_detect_revocations__mutmut_67: survived
--- src/engines/financial_events/lineage_walker.py
+++ src/engines/financial_events/lineage_walker.py
@@ -43,7 +43,7 @@
         event_id = event.get("id", 0)
         transfer_id = event.get("transfer_id", "")
         date_iso = event.get("date_iso", "")
-        lifecycle_state = event.get("lifecycle_state", "")
+        lifecycle_state = event.get("lifecycle_state", "XXXX")
 
         # Only process revocation events
         if event_type != "transfer_revocation" or lifecycle_state != "open":
