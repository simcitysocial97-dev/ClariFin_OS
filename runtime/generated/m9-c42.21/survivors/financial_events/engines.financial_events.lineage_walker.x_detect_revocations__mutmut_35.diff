# status: survived
# engines.financial_events.lineage_walker.x_detect_revocations__mutmut_35: survived
--- src/engines/financial_events/lineage_walker.py
+++ src/engines/financial_events/lineage_walker.py
@@ -39,7 +39,7 @@
     processed_revocations: set[tuple[int, int]] = set()
 
     for event in events:
-        event_type = event.get("event_type", "")
+        event_type = event.get("event_type", "XXXX")
         event_id = event.get("id", 0)
         transfer_id = event.get("transfer_id", "")
         date_iso = event.get("date_iso", "")
