# status: survived
# engines.financial_events.lineage_walker.x_detect_rollover_scenarios__mutmut_6: survived
--- src/engines/financial_events/lineage_walker.py
+++ src/engines/financial_events/lineage_walker.py
@@ -22,7 +22,7 @@
     # For now, we'll detect rollovers based on temporal patterns
 
     for event in events:
-        event_type = event.get("event_type", "")
+        event_type = event.get("event_type", None)
         event_id = event.get("id", 0)
         event.get("account_id", "")
         date_iso = event.get("date_iso", "")
