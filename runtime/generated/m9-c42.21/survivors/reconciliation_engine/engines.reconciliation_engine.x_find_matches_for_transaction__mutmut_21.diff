# status: survived
# engines.reconciliation_engine.x_find_matches_for_transaction__mutmut_21: survived
--- src/engines/reconciliation_engine.py
+++ src/engines/reconciliation_engine.py
@@ -34,7 +34,7 @@
 
     target_dict = dict(target)
     target_debit = target_dict.get("debit") or 0
-    target_credit = target_dict.get("credit") or 0
+    target_credit = target_dict.get(None) or 0
 
     # Find potential matching transactions
     # Must be opposite type with same amount
