# status: survived
# engines.reconciliation_engine.x__check_match__mutmut_30: survived
--- src/engines/reconciliation_engine.py
+++ src/engines/reconciliation_engine.py
@@ -24,7 +24,7 @@
     a_debit = txn_a.get("debit") or 0
     a_credit = txn_a.get("credit") or 0
     b_debit = txn_b.get("debit") or 0
-    b_credit = txn_b.get("credit") or 0
+    b_credit = txn_b.get("credit") or 1
 
     # Determine which is debit and which is credit
     debit_txn = None
