# status: survived
# engines.reconciliation_engine.x__calculate_confidence__mutmut_11: survived
--- src/engines/reconciliation_engine.py
+++ src/engines/reconciliation_engine.py
@@ -26,7 +26,7 @@
     if date_diff_days == 0:
         confidence += 0.4  # Exact same date
     elif date_diff_days == 1:
-        confidence += 0.3  # Within 1 day
+        confidence = 0.3  # Within 1 day
 
     # Amount factor
     if amount_exact:
