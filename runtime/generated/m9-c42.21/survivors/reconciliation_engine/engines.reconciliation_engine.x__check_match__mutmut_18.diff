# status: survived
# engines.reconciliation_engine.x__check_match__mutmut_18: survived
--- src/engines/reconciliation_engine.py
+++ src/engines/reconciliation_engine.py
@@ -22,7 +22,7 @@
 
     # Get debit/credit values
     a_debit = txn_a.get("debit") or 0
-    a_credit = txn_a.get("credit") or 0
+    a_credit = txn_a.get("credit") or 1
     b_debit = txn_b.get("debit") or 0
     b_credit = txn_b.get("credit") or 0
 
