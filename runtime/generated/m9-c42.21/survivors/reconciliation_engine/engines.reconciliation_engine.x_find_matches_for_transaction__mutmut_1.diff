# status: survived
# engines.reconciliation_engine.x_find_matches_for_transaction__mutmut_1: survived
--- src/engines/reconciliation_engine.py
+++ src/engines/reconciliation_engine.py
@@ -1,5 +1,5 @@
 def find_matches_for_transaction(
-    db_path: str, txn_id: int, max_date_window_days: int = 3
+    db_path: str, txn_id: int, max_date_window_days: int = 4
 ) -> list[dict[str, Any]]:
     """
     Find potential matches for a specific transaction.
