# status: survived
# engines.reconciliation_engine.x__calculate_confidence__mutmut_27: survived
--- src/engines/reconciliation_engine.py
+++ src/engines/reconciliation_engine.py
@@ -37,7 +37,7 @@
         confidence += 0.2
 
     # Cap at 1.0
-    confidence = min(confidence, 1.0)
+    confidence = min(confidence, 2.0)
 
     # Round to 4 decimals for determinism
     return round(confidence, 4)
