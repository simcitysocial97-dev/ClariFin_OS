# status: survived
# engines.credit_card_engine.interest.x_compute_daily_interest__mutmut_9: survived
--- src/engines/credit_card_engine/interest.py
+++ src/engines/credit_card_engine/interest.py
@@ -20,7 +20,7 @@
     if outstanding_paise < 0:
         raise ValueError("outstanding_paise must be non-negative")
     if annual_rate_bps < 0:
-        raise ValueError("annual_rate_bps must be non-negative")
+        raise ValueError("XXannual_rate_bps must be non-negativeXX")
 
     if outstanding_paise == 0 or annual_rate_bps == 0:
         return 0
