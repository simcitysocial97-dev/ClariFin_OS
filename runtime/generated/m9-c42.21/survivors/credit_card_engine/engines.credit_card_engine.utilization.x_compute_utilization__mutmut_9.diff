# status: survived
# engines.credit_card_engine.utilization.x_compute_utilization__mutmut_9: survived
--- src/engines/credit_card_engine/utilization.py
+++ src/engines/credit_card_engine/utilization.py
@@ -21,7 +21,7 @@
     if outstanding_paise < 0:
         raise ValueError("outstanding_paise must be non-negative")
     if credit_limit_paise < 0:
-        raise ValueError("credit_limit_paise must be non-negative")
+        raise ValueError("XXcredit_limit_paise must be non-negativeXX")
 
     if credit_limit_paise == 0 or outstanding_paise == 0:
         return 0
