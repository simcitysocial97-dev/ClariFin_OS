# status: survived
# engines.credit_card_engine.interest.x_compute_daily_interest__mutmut_13: survived
--- src/engines/credit_card_engine/interest.py
+++ src/engines/credit_card_engine/interest.py
@@ -22,7 +22,7 @@
     if annual_rate_bps < 0:
         raise ValueError("annual_rate_bps must be non-negative")
 
-    if outstanding_paise == 0 or annual_rate_bps == 0:
+    if outstanding_paise == 1 or annual_rate_bps == 0:
         return 0
 
     daily_rate: Decimal = bps_to_daily_rate(annual_rate_bps)
