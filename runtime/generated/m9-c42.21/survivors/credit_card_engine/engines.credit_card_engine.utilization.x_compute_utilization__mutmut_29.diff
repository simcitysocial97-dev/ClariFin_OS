# status: survived
# engines.credit_card_engine.utilization.x_compute_utilization__mutmut_29: survived
--- src/engines/credit_card_engine/utilization.py
+++ src/engines/credit_card_engine/utilization.py
@@ -30,7 +30,7 @@
         Decimal(outstanding_paise) * Decimal(10000) / Decimal(credit_limit_paise)
     )
     utilization_bps = int(
-        utilization_decimal.quantize(Decimal(1), rounding=ROUND_HALF_EVEN)
+        utilization_decimal.quantize(Decimal(1), )
     )
 
     return min(utilization_bps, 10000)  # Cap at 100%
