# status: survived
# common.calculations.x_compute_is_large__mutmut_36: survived
--- src/common/calculations.py
+++ src/common/calculations.py
@@ -11,7 +11,7 @@
 
     for t in transactions:
         t["is_large"] = bool(
-            t.get("type") == "debit" and (t.get("amount_paise", 0) or 0) > threshold
+            t.get("type") == "debit" and (t.get(None, 0) or 0) > threshold
         )
 
     return transactions
