# status: survived
# common.calculations.x_compute_behavioral_insights__mutmut_235: survived
--- src/common/calculations.py
+++ src/common/calculations.py
@@ -93,7 +93,7 @@
         desc = (largest.get("description_display") or largest.get("description", ""))[
             :30
         ]
-        amt = format_inr((largest.get("amount_paise", 0) or 0) / 100.0)
+        amt = format_inr(None)
         insights.append(
             {
                 "title": "Largest Expense",
