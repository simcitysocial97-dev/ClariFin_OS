# status: survived
# common.calculations.x_compute_behavioral_insights__mutmut_155: survived
--- src/common/calculations.py
+++ src/common/calculations.py
@@ -63,7 +63,7 @@
         other_totals = [v for k, v in monthly_totals.items() if k != this_month]
         if other_totals:
             avg_other_total = sum(other_totals) / len(other_totals)
-            if avg_other_total > 0:
+            if avg_other_total > 1:
                 pct_change_total = (
                     (this_month_total - avg_other_total) / avg_other_total
                 ) * 100
