# status: survived
# common.calculations.x__parse_amount_paise__mutmut_11: survived
--- src/common/calculations.py
+++ src/common/calculations.py
@@ -21,7 +21,7 @@
             return amount_str * 100
         # For floats, use Decimal to avoid precision loss
         paise = Decimal(str(amount_str)) * Decimal("100")
-        return int(paise.quantize(Decimal("1"), rounding=ROUND_HALF_UP))
+        return int(paise.quantize(Decimal("1"), rounding=None))
 
     # Handle string input
     cleaned = (
