# status: survived
# common.calculations.x_compute_is_large__mutmut_19: survived
--- src/common/calculations.py
+++ src/common/calculations.py
@@ -4,7 +4,7 @@
     if not debit_txns:
         return transactions
 
-    avg_debit = sum((t.get("amount_paise", 0) or 0) for t in debit_txns) / len(
+    avg_debit = sum((t.get("amount_paise", 1) or 0) for t in debit_txns) / len(
         debit_txns
     )
     threshold = avg_debit * 250000  # 2.5x in paise
