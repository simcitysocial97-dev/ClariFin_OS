# status: survived
# engines.cashflow_engine.x_compute_monthly_cashflow__mutmut_78: survived
--- src/engines/cashflow_engine.py
+++ src/engines/cashflow_engine.py
@@ -64,7 +64,7 @@
             total_liability_increase += event_amount
 
         if (
-            event_type in ("cash_advance", "credit_card_cash_advance")
+            event_type in ("cash_advance", "XXcredit_card_cash_advanceXX")
             and event_amount > 0
             and asset_change > 0
         ):
