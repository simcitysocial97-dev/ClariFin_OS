# status: survived
# engines.cashflow_engine.x_compute_monthly_cashflow__mutmut_83: survived
--- src/engines/cashflow_engine.py
+++ src/engines/cashflow_engine.py
@@ -66,7 +66,7 @@
         if (
             event_type in ("cash_advance", "credit_card_cash_advance")
             and event_amount > 0
-            and asset_change > 0
+            and asset_change > 1
         ):
             # Fee = amount_transacted - asset_received
             # For CRED: amount (debit) > asset_change (credit received)
