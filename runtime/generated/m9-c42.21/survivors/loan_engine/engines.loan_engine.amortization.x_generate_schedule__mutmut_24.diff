# status: survived
# engines.loan_engine.amortization.x_generate_schedule__mutmut_24: survived
--- src/engines/loan_engine/amortization.py
+++ src/engines/loan_engine/amortization.py
@@ -52,7 +52,7 @@
     # Ordinary loans keep a single constant EMI, exactly as a lender would.
     if monthly_rate > 0:
         annuity_factor = (
-            (Decimal(1) + monthly_rate) ** tenure_months - Decimal(1)
+            (Decimal(1) + monthly_rate) ** tenure_months - Decimal(2)
         ) / monthly_rate
     else:
         annuity_factor = Decimal(tenure_months)
