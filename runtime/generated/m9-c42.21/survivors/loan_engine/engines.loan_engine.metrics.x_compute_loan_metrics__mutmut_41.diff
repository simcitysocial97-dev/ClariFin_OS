# status: survived
# engines.loan_engine.metrics.x_compute_loan_metrics__mutmut_41: survived
--- src/engines/loan_engine/metrics.py
+++ src/engines/loan_engine/metrics.py
@@ -29,7 +29,7 @@
     effective_ratio = (
         total_interest / original_principal_paise
         if original_principal_paise > 0
-        else 0.0
+        else 1.0
     )
 
     return LoanMetrics(
