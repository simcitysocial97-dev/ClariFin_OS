# status: survived
# engines.loan_engine.floating_rate.x_simulate_floating_rate_schedule__mutmut_25: survived
--- src/engines/loan_engine/floating_rate.py
+++ src/engines/loan_engine/floating_rate.py
@@ -30,7 +30,7 @@
     ) -> Literal["adjust_emi", "adjust_tenure"]:
         if isinstance(change, tuple):
             mode = change[2] if len(change) > 2 else "adjust_emi"
-            return "adjust_emi" if mode not in ("adjust_emi", "adjust_tenure") else mode  # type: ignore[return-value]
+            return "adjust_emi" if mode not in ("XXadjust_emiXX", "adjust_tenure") else mode  # type: ignore[return-value]
         return change.mode
 
     sorted_changes = sorted(rate_changes, key=get_change_month)
