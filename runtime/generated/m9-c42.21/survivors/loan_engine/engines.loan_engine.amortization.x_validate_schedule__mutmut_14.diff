# status: survived
# engines.loan_engine.amortization.x_validate_schedule__mutmut_14: survived
--- src/engines/loan_engine/amortization.py
+++ src/engines/loan_engine/amortization.py
@@ -54,7 +54,7 @@
         )
 
     # 4. EMI consistency (same EMI for all rows except last)
-    if len(schedule) > 1:
+    if len(schedule) >= 1:
         first_emi = schedule[0].emi_paise
         for row in schedule[:-1]:  # exclude last row
             if row.emi_paise != first_emi:
