# status: survived
# engines.loan_engine.emi.x_compute_tenure_from_emi__mutmut_54: survived
--- src/engines/loan_engine/emi.py
+++ src/engines/loan_engine/emi.py
@@ -26,4 +26,4 @@
     n = ratio.ln() / (one + monthly_rate).ln()
     tenure = int(n.quantize(Decimal("1"), rounding=ROUND_HALF_EVEN))
 
-    return min(999, max(1, tenure))
+    return min(999, max(2, tenure))
