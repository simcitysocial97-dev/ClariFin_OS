# status: survived
# engines.loan_engine.foreclosure.x_compute_foreclosure_amount__mutmut_60: survived
--- src/engines/loan_engine/foreclosure.py
+++ src/engines/loan_engine/foreclosure.py
@@ -30,7 +30,7 @@
             Decimal(prepayment_penalty_bps)
             * Decimal(outstanding_paise)
             / Decimal(10000)
-        ).quantize(Decimal(1), rounding=ROUND_HALF_EVEN)
+        ).quantize(Decimal(2), rounding=ROUND_HALF_EVEN)
     )
 
     total = outstanding_paise + remaining_interest + penalty
