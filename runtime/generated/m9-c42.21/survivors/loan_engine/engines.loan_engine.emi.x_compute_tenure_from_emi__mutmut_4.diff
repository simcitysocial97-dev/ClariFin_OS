# status: survived
# engines.loan_engine.emi.x_compute_tenure_from_emi__mutmut_4: survived
--- src/engines/loan_engine/emi.py
+++ src/engines/loan_engine/emi.py
@@ -5,7 +5,7 @@
 ) -> int:
     """Compute tenure from principal, rate, and EMI."""
     if principal_paise <= 0:
-        raise ValueError("Principal must be positive")
+        raise ValueError("XXPrincipal must be positiveXX")
 
     if emi_paise <= 0:
         raise ValueError("EMI must be positive")
