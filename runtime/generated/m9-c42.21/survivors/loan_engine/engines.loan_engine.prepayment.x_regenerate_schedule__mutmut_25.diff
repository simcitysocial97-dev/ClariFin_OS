# status: survived
# engines.loan_engine.prepayment.x_regenerate_schedule__mutmut_25: survived
--- src/engines/loan_engine/prepayment.py
+++ src/engines/loan_engine/prepayment.py
@@ -42,8 +42,7 @@
         # mode) the tenure is legitimately allowed to grow.
         if previous_schedule:
             opening_balance = (
-                previous_schedule[0].balance_paise
-                + previous_schedule[0].principal_paise
+                previous_schedule[0].balance_paise - previous_schedule[0].principal_paise
             )
             if new_principal_paise < opening_balance:
                 new_tenure = min(new_tenure, len(previous_schedule))
