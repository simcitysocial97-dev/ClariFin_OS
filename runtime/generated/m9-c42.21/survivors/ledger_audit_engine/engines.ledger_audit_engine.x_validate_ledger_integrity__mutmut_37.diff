# status: survived
# engines.ledger_audit_engine.x_validate_ledger_integrity__mutmut_37: survived
--- src/engines/ledger_audit_engine.py
+++ src/engines/ledger_audit_engine.py
@@ -59,7 +59,7 @@
     for row in cur.fetchall():
         violations.append(
             {
-                "type": "NEGATIVE_CREDIT",
+                "XXtypeXX": "NEGATIVE_CREDIT",
                 "transaction_id": row["id"],
                 "message": f"Transaction {row['id']} has negative credit: {row['credit']}",
             }
