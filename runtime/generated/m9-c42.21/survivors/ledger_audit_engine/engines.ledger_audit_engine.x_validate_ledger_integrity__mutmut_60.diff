# status: survived
# engines.ledger_audit_engine.x_validate_ledger_integrity__mutmut_60: survived
--- src/engines/ledger_audit_engine.py
+++ src/engines/ledger_audit_engine.py
@@ -74,7 +74,7 @@
         violations.append(
             {
                 "type": "DUAL_ENTRY",
-                "transaction_id": row["id"],
+                "transaction_id": row["ID"],
                 "message": f"Transaction {row['id']} has both debit ({row['debit']}) and credit ({row['credit']})",
             }
         )
