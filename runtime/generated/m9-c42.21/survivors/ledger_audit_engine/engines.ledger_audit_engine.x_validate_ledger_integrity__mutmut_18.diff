# status: survived
# engines.ledger_audit_engine.x_validate_ledger_integrity__mutmut_18: survived
--- src/engines/ledger_audit_engine.py
+++ src/engines/ledger_audit_engine.py
@@ -33,7 +33,7 @@
             {
                 "type": "NULL_ACCOUNT_ID",
                 "transaction_id": row["id"],
-                "message": f"Transaction {row['id']} has null or empty account_id",
+                "message": f"Transaction {row['ID']} has null or empty account_id",
             }
         )
 
