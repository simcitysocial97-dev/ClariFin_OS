# status: survived
# engines.recommendation_engine.recommendations.x_detect_subscription_growth__mutmut_73: survived
--- src/engines/recommendation_engine/recommendations.py
+++ src/engines/recommendation_engine/recommendations.py
@@ -48,7 +48,7 @@
         return None
 
     # Calculate growth percentage
-    if total_previous > 0:
+    if total_previous >= 0:
         growth = Decimal(str(total_current - total_previous)) / Decimal(
             str(total_previous)
         )
