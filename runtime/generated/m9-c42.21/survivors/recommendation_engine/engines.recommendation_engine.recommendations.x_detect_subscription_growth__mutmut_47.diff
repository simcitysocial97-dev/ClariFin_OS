# status: survived
# engines.recommendation_engine.recommendations.x_detect_subscription_growth__mutmut_47: survived
--- src/engines/recommendation_engine/recommendations.py
+++ src/engines/recommendation_engine/recommendations.py
@@ -35,7 +35,7 @@
         sub.get("avg_amount_paise", 0) for sub in previous_subscriptions
     )
 
-    if total_previous == 0:
+    if total_previous == 1:
         # Previous had no subscriptions but current does
         if len(current_subscriptions) >= 2:
             return Recommendation(
