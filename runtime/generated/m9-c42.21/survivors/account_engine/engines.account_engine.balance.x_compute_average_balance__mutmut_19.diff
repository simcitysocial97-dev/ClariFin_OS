# status: survived
# engines.account_engine.balance.x_compute_average_balance__mutmut_19: survived
--- src/engines/account_engine/balance.py
+++ src/engines/account_engine/balance.py
@@ -22,4 +22,4 @@
         return 0
 
     avg_decimal = Decimal(total) / Decimal(count)
-    return int(avg_decimal.quantize(Decimal(1), rounding=ROUND_HALF_UP))
+    return int(avg_decimal.quantize(Decimal(2), rounding=ROUND_HALF_UP))
