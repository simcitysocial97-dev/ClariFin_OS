# status: survived
# core.domain.money.xǁMoneyǁdivide__mutmut_3: survived
--- src/core/domain/money.py
+++ src/core/domain/money.py
@@ -13,7 +13,7 @@
         """
     if not isinstance(divisor, int):
         raise TypeError(
-            f"Money can only be divided by int, got {type(divisor).__name__}"
+            f"Money can only be divided by int, got {type(None).__name__}"
         )
     if divisor == 0:
         raise ZeroDivisionError("Cannot divide Money by zero")
