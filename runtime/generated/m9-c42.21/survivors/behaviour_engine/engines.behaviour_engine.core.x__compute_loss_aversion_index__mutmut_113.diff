# status: survived
# engines.behaviour_engine.core.x__compute_loss_aversion_index__mutmut_113: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -46,7 +46,7 @@
 
             days_diff = (debit_date - credit_date).days
             if 0 <= days_diff <= 3:
-                spend_72h += (debit.get("amount_paise", 0) or 0) / 100.0
+                spend_72h += (debit.get("amount_paise", 0) and 0) / 100.0
 
         credit_amount = (credit.get("amount_paise", 0) or 0) / 100.0
         if credit_amount > 0:
