# status: survived
# engines.behaviour_engine.core.x__compute_habit_stability_score__mutmut_145: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -67,7 +67,7 @@
     # Behavioral rhythm (regular transaction days)
     daily_counts: defaultdict[str, float] = defaultdict(float)
     for txn in debits:
-        date_iso = txn.get("date_iso", "")
+        date_iso = txn.get("date_iso", )
         if date_iso:
             daily_counts[date_iso] += 1
 
