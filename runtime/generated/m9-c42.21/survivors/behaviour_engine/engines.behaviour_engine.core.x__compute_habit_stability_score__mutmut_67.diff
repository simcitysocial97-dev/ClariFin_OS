# status: survived
# engines.behaviour_engine.core.x__compute_habit_stability_score__mutmut_67: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -34,7 +34,7 @@
     if monthly_category:
         all_categories: set[str] = set()
         for month_data in monthly_category.values():
-            all_categories.update(month_data.keys())
+            all_categories.update(None)
 
         for cat in all_categories:
             monthly_vals = [
