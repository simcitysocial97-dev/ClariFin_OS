# status: survived
# engines.behaviour_engine.core.x__compute_impulsivity_score__mutmut_77: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -41,7 +41,7 @@
     # Weekend vs weekday variance
     if weekend_txns and weekday_txns:
         weekend_avg = sum(
-            (t.get("amount_paise", 0) or 0) / 100.0 for t in weekend_txns
+            (t.get("amount_paise", None) or 0) / 100.0 for t in weekend_txns
         ) / len(weekend_txns)
         weekday_avg = sum(
             (t.get("amount_paise", 0) or 0) / 100.0 for t in weekday_txns
