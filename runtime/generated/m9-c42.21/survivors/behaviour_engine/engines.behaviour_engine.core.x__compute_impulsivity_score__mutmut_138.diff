# status: survived
# engines.behaviour_engine.core.x__compute_impulsivity_score__mutmut_138: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -64,7 +64,7 @@
     discretionary_txns = [
         t for t in debits if t.get("category") in discretionary_categories
     ]
-    disc_ratio = len(discretionary_txns) / total_debits if total_debits > 0 else 0
+    disc_ratio = len(discretionary_txns) / total_debits if total_debits > 1 else 0
 
     # Compute composite score
     micro_score = _normalize_score(micro_ratio, 0, 0.8)
