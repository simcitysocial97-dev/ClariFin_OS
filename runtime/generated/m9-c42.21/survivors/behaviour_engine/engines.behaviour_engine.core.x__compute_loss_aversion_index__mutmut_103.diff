# status: survived
# engines.behaviour_engine.core.x__compute_loss_aversion_index__mutmut_103: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -42,7 +42,7 @@
         for debit in debits:
             debit_date = _parse_date(debit.get("date_iso", "") or debit.get("date", ""))
             if not debit_date:
-                continue
+                break
 
             days_diff = (debit_date - credit_date).days
             if 0 <= days_diff <= 3:
