# status: survived
# engines.behaviour_engine.core.x__compute_loss_aversion_index__mutmut_228: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -84,5 +84,5 @@
         "score": round(loss_aversion, 4),
         "post_income_velocity": round(avg_velocity, 4),
         "recovery_time_days": recovery_days,
-        "large_expense_count": len(large_expenses),
+        "XXlarge_expense_countXX": len(large_expenses),
     }
