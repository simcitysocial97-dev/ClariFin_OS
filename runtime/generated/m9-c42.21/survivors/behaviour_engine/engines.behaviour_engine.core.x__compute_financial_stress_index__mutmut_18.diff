# status: survived
# engines.behaviour_engine.core.x__compute_financial_stress_index__mutmut_18: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -15,7 +15,7 @@
 
     # Separate credits and debits
     credits = [t for t in transactions if t.get("type") == "credit"]
-    debits = [t for t in transactions if t.get("type") == "debit"]
+    debits = None
 
     if not debits:
         return {"score": 0.5, "balance_volatility": 0.0, "credit_dependency": 0.0}
