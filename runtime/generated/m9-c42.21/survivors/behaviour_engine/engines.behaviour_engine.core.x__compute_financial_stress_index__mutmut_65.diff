# status: survived
# engines.behaviour_engine.core.x__compute_financial_stress_index__mutmut_65: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -29,7 +29,7 @@
             if txn.get("type") == "debit":
                 daily_net[date_iso] -= amount
             else:
-                daily_net[date_iso] += amount
+                daily_net[date_iso] = amount
 
     # Running balance simulation
     sorted_dates = sorted(daily_net.keys())
