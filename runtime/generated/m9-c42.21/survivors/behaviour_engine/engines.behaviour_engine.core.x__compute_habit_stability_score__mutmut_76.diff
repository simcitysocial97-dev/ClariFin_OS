# status: survived
# engines.behaviour_engine.core.x__compute_habit_stability_score__mutmut_76: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -40,7 +40,7 @@
             monthly_vals = [
                 monthly_category[m].get(cat, 0) for m in sorted(monthly_category.keys())
             ]
-            if len(monthly_vals) >= 2:
+            if len(monthly_vals) >= 3:
                 cv = _coefficient_of_variation(monthly_vals)
                 category_cvs.append(cv)
 
