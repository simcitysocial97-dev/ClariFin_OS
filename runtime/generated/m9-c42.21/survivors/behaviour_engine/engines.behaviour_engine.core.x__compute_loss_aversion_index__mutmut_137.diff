# status: survived
# engines.behaviour_engine.core.x__compute_loss_aversion_index__mutmut_137: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -50,7 +50,7 @@
 
         credit_amount = (credit.get("amount_paise", 0) or 0) / 100.0
         if credit_amount > 0:
-            velocity = spend_72h / credit_amount
+            velocity = None
             post_income_spends.append(velocity)
 
     # Calculate average velocity
