# status: survived
# engines.behaviour_engine.core.x__compute_impulsivity_score__mutmut_104: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -46,7 +46,7 @@
         weekday_avg = sum(
             (t.get("amount_paise", 0) or 0) / 100.0 for t in weekday_txns
         ) / len(weekday_txns)
-        weekend_ratio = weekend_avg / max(weekday_avg, 1)
+        weekend_ratio = weekend_avg / max(weekday_avg, )
     else:
         weekend_ratio = 1.0
 
