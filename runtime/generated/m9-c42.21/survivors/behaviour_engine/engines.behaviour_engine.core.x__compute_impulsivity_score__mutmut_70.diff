# status: survived
# engines.behaviour_engine.core.x__compute_impulsivity_score__mutmut_70: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -39,7 +39,7 @@
                 pass
 
     # Weekend vs weekday variance
-    if weekend_txns and weekday_txns:
+    if weekend_txns or weekday_txns:
         weekend_avg = sum(
             (t.get("amount_paise", 0) or 0) / 100.0 for t in weekend_txns
         ) / len(weekend_txns)
