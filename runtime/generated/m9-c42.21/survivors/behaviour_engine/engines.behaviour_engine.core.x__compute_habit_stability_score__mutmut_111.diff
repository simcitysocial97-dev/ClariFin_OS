# status: survived
# engines.behaviour_engine.core.x__compute_habit_stability_score__mutmut_111: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -54,7 +54,7 @@
         if amount > 0:
             desc_amounts[desc].append(amount)
 
-    recurring_count = 0
+    recurring_count = 1
     for _desc, amounts in desc_amounts.items():
         if len(amounts) >= 3:
             # Check if amounts are similar (within 10%)
