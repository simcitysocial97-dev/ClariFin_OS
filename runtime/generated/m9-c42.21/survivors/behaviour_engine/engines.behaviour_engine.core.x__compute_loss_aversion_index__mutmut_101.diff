# status: survived
# engines.behaviour_engine.core.x__compute_loss_aversion_index__mutmut_101: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -40,7 +40,7 @@
         # Find debits within 72 hours
         spend_72h = 0.0
         for debit in debits:
-            debit_date = _parse_date(debit.get("date_iso", "") or debit.get("date", ""))
+            debit_date = _parse_date(debit.get("date_iso", "") or debit.get("date", "XXXX"))
             if not debit_date:
                 continue
 
