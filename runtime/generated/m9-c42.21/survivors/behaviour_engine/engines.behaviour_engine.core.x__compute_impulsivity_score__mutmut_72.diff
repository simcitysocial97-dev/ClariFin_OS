# status: survived
# engines.behaviour_engine.core.x__compute_impulsivity_score__mutmut_72: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -42,7 +42,7 @@
     if weekend_txns and weekday_txns:
         weekend_avg = sum(
             (t.get("amount_paise", 0) or 0) / 100.0 for t in weekend_txns
-        ) / len(weekend_txns)
+        ) * len(weekend_txns)
         weekday_avg = sum(
             (t.get("amount_paise", 0) or 0) / 100.0 for t in weekday_txns
         ) / len(weekday_txns)
