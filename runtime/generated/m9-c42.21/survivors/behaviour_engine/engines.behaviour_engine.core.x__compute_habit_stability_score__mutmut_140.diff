# status: survived
# engines.behaviour_engine.core.x__compute_habit_stability_score__mutmut_140: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -65,7 +65,7 @@
     recurring_score = min(1.0, recurring_count / 10)  # Normalize to max 10 recurring
 
     # Behavioral rhythm (regular transaction days)
-    daily_counts: defaultdict[str, float] = defaultdict(float)
+    daily_counts: defaultdict[str, float] = defaultdict(None)
     for txn in debits:
         date_iso = txn.get("date_iso", "")
         if date_iso:
