# status: survived
# engines.behaviour_engine.core.x__compute_habit_stability_score__mutmut_12: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -12,7 +12,7 @@
     if not transactions:
         return {"score": 0.5, "category_cv": 0.0, "recurring_predictability": 0.0}
 
-    debits = [t for t in transactions if t.get("type") == "debit"]
+    debits = [t for t in transactions if t.get(None) == "debit"]
     if not debits:
         return {"score": 0.5, "category_cv": 0.0, "recurring_predictability": 0.0}
 
