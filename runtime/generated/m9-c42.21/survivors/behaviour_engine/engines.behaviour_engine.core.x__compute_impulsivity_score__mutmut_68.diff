# status: survived
# engines.behaviour_engine.core.x__compute_impulsivity_score__mutmut_68: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -32,7 +32,7 @@
             try:
                 dt = datetime.strptime(date_iso, "%Y-%m-%d")
                 if dt.weekday() >= 5:  # Saturday, Sunday
-                    weekend_txns.append(txn)
+                    weekend_txns.append(None)
                 else:
                     weekday_txns.append(txn)
             except (ValueError, TypeError):
