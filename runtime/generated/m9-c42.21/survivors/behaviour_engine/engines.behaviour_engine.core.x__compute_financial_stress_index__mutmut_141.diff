# status: survived
# engines.behaviour_engine.core.x__compute_financial_stress_index__mutmut_141: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -57,7 +57,7 @@
     for txn in debits:
         date_iso = txn.get("date_iso", "")
         amount = (txn.get("amount_paise", 0) or 0) / 100.0
-        total_spending += amount
+        total_spending -= amount
 
         if date_iso:
             try:
