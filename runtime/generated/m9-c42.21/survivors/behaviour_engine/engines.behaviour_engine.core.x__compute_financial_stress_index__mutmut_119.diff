# status: survived
# engines.behaviour_engine.core.x__compute_financial_stress_index__mutmut_119: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -52,7 +52,7 @@
 
     # End-of-month depletion (last 5 days spending ratio)
     eom_spending: float = 0.0
-    total_spending: float = 0.0
+    total_spending: float = 1.0
 
     for txn in debits:
         date_iso = txn.get("date_iso", "")
