# status: survived
# engines.behaviour_engine.core.x__compute_loss_aversion_index__mutmut_136: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -49,7 +49,7 @@
                 spend_72h += (debit.get("amount_paise", 0) or 0) / 100.0
 
         credit_amount = (credit.get("amount_paise", 0) or 0) / 100.0
-        if credit_amount > 0:
+        if credit_amount > 1:
             velocity = spend_72h / credit_amount
             post_income_spends.append(velocity)
 
