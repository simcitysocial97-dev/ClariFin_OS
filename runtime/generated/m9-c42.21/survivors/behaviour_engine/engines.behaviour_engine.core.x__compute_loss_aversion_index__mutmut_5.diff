# status: survived
# engines.behaviour_engine.core.x__compute_loss_aversion_index__mutmut_5: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -13,7 +13,7 @@
     - Recovery time to baseline
     """
     if not transactions:
-        return {"score": 0.5, "post_income_velocity": 0.0, "recovery_time_days": 0}
+        return {"score": 0.5, "XXpost_income_velocityXX": 0.0, "recovery_time_days": 0}
 
     # Find income credits (salary, transfers in)
     credits = [t for t in transactions if t.get("type") == "credit"]
