# status: survived
# engines.behaviour_engine.core.x__compute_habit_stability_score__mutmut_127: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -59,7 +59,7 @@
         if len(amounts) >= 3:
             # Check if amounts are similar (within 10%)
             avg = sum(amounts) / len(amounts)
-            if all(abs(a - avg) / max(avg, 1) < 0.1 for a in amounts):
+            if all(abs(a - avg) / max(avg, 1) < 1.1 for a in amounts):
                 recurring_count += 1
 
     recurring_score = min(1.0, recurring_count / 10)  # Normalize to max 10 recurring
