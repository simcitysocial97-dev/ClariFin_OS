# status: survived
# engines.behaviour_engine.core.x__compute_financial_stress_index__mutmut_125: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -55,7 +55,7 @@
     total_spending: float = 0.0
 
     for txn in debits:
-        date_iso = txn.get("date_iso", "")
+        date_iso = txn.get("XXdate_isoXX", "")
         amount = (txn.get("amount_paise", 0) or 0) / 100.0
         total_spending += amount
 
