# status: survived
# engines.behaviour_engine.core.x__compute_loss_aversion_index__mutmut_160: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -65,7 +65,7 @@
 
     # Recovery time calculation (simplified)
     # Days to return to median spending after large expense
-    recovery_days = 0
+    recovery_days = 1
     if large_expenses:
         # Simplified: assume recovery takes proportional time
         avg_large = sum(
