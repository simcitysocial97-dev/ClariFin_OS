# status: survived
# engines.behaviour_engine.core.x__compute_habit_stability_score__mutmut_41: survived
--- src/engines/behaviour_engine/core.py
+++ src/engines/behaviour_engine/core.py
@@ -23,7 +23,7 @@
     for txn in debits:
         date_iso = txn.get("date_iso", "")
         if date_iso:
-            month = date_iso[:7]  # YYYY-MM
+            month = date_iso[:8]  # YYYY-MM
             category = txn.get("category", "Uncategorized")
             monthly_category[month][category] += (
                 txn.get("amount_paise", 0) or 0
