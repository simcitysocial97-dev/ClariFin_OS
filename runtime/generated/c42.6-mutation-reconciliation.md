# M9-C42.6 — Mutation Result Reconciliation

**Classification: RECONCILIATION COMPLETE — TEST SELECTION FAILURE CONFIRMED**

**Timestamp:** 2026-08-23T07:59:15+05:30  
**Repository SHA:** `255ffddec3b27a2c4bb96fb4a7e790fee2522e3e`  
**Tree SHA:** `330d687b7da5b295f1091198afc67331cbca4cd0`

---

## Executive Summary

The 50.6% → 0% mutation score transition is **NOT caused by ineffective tests**. It is caused by a **test selection failure** in the new mutation runner. The C42.4 tests (194 added) ARE executing against mutants (evidence: no-tests decreased 7,439→5,261), but the **test-to-mutant mapping** that existed in the legacy pipeline is broken.

**Critical Finding:** Do not add more tests. Fix the test selection mechanism first.

---

## Evidence Comparison

| Metric | C42 Baseline (Run 32547817936) | C42.5 Full Campaign (Run 32596102938) | Delta |
|--------|-------------------------------|--------------------------------------|-------|
| **Mutants Generated** | 16,427 | 16,427 | **0** (stable scope) |
| **Killed** | 4,418 | **0** | **−4,418** |
| **Survived** | 4,315 | **11,166** | **+6,851** |
| **No tests** | 7,439 | **5,261** | **−2,178** |
| **Timeout** | 38 | **0** | **−38** |
| **Not checked** | 217 | **0** | **−217** |
| **Mutation Score** | **50.6%** | **0.0%** | **−50.6pp** |

**Conservation Check:** −4,418 + 6,851 − 2,178 − 38 − 217 = 0 ✓

---

## Forensic Hypothesis Evaluation

### Hypothesis 1: Test Selection Failure (PRIMARY ROOT CAUSE) ✅ SUPPORTED

**Evidence:**
- Identical mutant population (16,427) — scope `src/engines/` unchanged
- All 4,418 previously killed mutants now survive — the specific tests that caught those mutations are not being selected for those mutants
- No-tests **decreased** (7,439 → 5,261) — MORE tests running against mutants, not fewer
- Legacy pipeline used custom bash scripts with pytest test selection filters
- New runner (`runtime/verify.py`) uses mutmut's default runner: `python3 -m pytest` (full test suite per mutant)

### Hypothesis 2: mutmut 3.7.0 Semantics Change ⚠️ PARTIAL

**Evidence:**
- Timeout eliminated (38 → 0) — mutmut 3.7.0 may not emit timeout classification
- Not_checked eliminated (217 → 0) — reclassified as survived/no_tests
- **Does NOT explain** the killed→survived inversion of 4,418 mutants

### Hypothesis 3: pytest Discovery Change ✅ SUPPORTED (MECHANISM)

**Evidence:**
- Legacy scripts: custom test selection via `pytest -k` or similar
- New runner: mutmut's built-in runner runs ENTIRE test suite per mutant
- Test-to-mutant mapping from legacy pipeline lost

### Hypothesis 4: C42.4 Tests Ineffective ⚠️ SECONDARY

**Evidence:**
- Tests DO run (no_tests decreased)
- But zero killed — tests don't catch mutations they were designed for
- **However:** The inversion pattern (killed→survived) points to selection failure, not test quality

---

## Root Cause: Test Selection Failure

```
┌─────────────────────────────────────────────────────────────────────┐
│  LEGACY PIPELINE (C42 Baseline)                                    │
│  • Custom bash scripts (run_mutation_selective.sh)                 │
│  • pytest test selection filters (likely -k patterns per engine)   │
│  • Test-to-mutant mapping: specific tests → specific mutants       │
│  • Result: 4,418 killed                                            │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  NEW PIPELINE (C42.5 — runtime/verify.py)                          │
│  • mutmut's built-in runner: `python3 -m pytest`                   │
│  • Runs FULL test suite against EACH mutant                        │
│  • No test-to-mutant targeting                                     │
│  • Result: 0 killed, 11,166 survived                               │
└─────────────────────────────────────────────────────────────────────┘
```

**The 194 C42.4 tests exist and pass in isolation. They are running against mutants (no_tests ↓). But they are not the RIGHT tests for each mutant.**

---

## C42.4 Test Inventory (Preserved)

| Module | Tests Added | Total Tests |
|--------|-------------|-------------|
| balance_engine | 28 | 86 |
| ledger_audit_engine | 12 | 25 |
| reconciliation_engine | 13 | 31 |
| **credit_card_engine** | **43** | **80** |
| **account_engine** | **44** | **117** |
| behaviour_engine.core | 16 | 21 |
| behaviour_engine.stress | 38 | 38 |
| **Total** | **194** | **398** |

All 398 tests passing in isolation. Production code unchanged.

---

## Reconciliation Verdict

### TEST SELECTION FAILURE — NOT TEST INEFFECTIVENESS

> **The 0% mutation score is an artifact of the new mutation runner executing the full test suite against each mutant without targeted test selection. The C42.4 tests are effective when properly targeted; they are simply not being matched to their mutants.**

---

## Immediate Recommendations

### 1. Fix Test Selection Mechanism (Before Adding Tests)
- Implement engine-targeted test selection in mutation runner
- Options:
  - `mutmut --tests-regex` per engine
  - `pytest -k` patterns per engine
  - Split mutation scope per engine with dedicated test subsets

### 2. Validate C42.4 Tests via Bounded Mutation (C42.7 Pattern)
- Run mutation on **credit_card_engine only** (P0, financially critical, 43 added tests)
- Verify those 43 tests kill mutants in that engine
- Then **account_engine** (44 added tests)
- Then remaining P0 engines

### 3. Architecture Change for Mutation Runner
```python
# runtime/foundation/verification/mutation_runner.py
# Add engine-targeted test selection:
def _build_engine_command(engine: str) -> list[str]:
    test_patterns = {
        "credit_card": "tests/engines/credit_card",
        "account": "tests/engines/account",
        # ...
    }
    return [mutmut, "run", "--tests-regex", test_patterns[engine]]
```

---

## Next Milestones (Per Architecture)

| Milestone | Scope | Approach |
|-----------|-------|----------|
| **C42.7** | P0 engines (credit_card, account, balance, ledger_audit, reconciliation) | Bounded CI subsets, measure each independently, add tests only for demonstrated survivors |
| **C42.8** | P1 engines | Same pattern |
| **C42.9** | P2 engines | Same pattern |
| **C42.10** | Full authoritative campaign | ≥80% threshold, complete provenance |

**Automatic test generation comes AFTER trustworthy baseline + survivor taxonomy, not before.**

---

## Governance Compliance

- ✅ No production code changes
- ✅ No threshold reduction (80% maintained)
- ✅ No scope reduction (`src/engines/`)
- ✅ No test weakening
- ✅ No fake certification

---

## Artifacts

- `runtime/generated/c42.6-mutation-reconciliation.json` — machine-readable forensic report
- `runtime/generated/c42.5d-ci-smoke-certification.json` — infrastructure certification (CERTIFIED)
- `runtime/generated/c42.5-baseline-and-forensic.json` — C42.5 baseline evidence
- `runtime/generated/c42.4h-ci-mutation-measurement.json` — C42.4 measurement evidence