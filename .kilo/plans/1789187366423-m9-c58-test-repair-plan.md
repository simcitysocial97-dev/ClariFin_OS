# M9-C58 — Verification Contract & Architecture Provider Reconciliation

**Date:** 2026-09-12
**Status:** COMPLETED
**Predecessor:** M9-C57 O-5-R2 (CERTIFIED, commit `10224f52`)
**Branch:** `m9c9-merge-authorization-resolution`
**Base commit:** `10224f52` (verified)
**Completion commit:** TBD

---

## Objective

Reconcile nine test failures and five contract drifts to **authoritative architecture/runtime truth**. Fix genuine framework defects. Explicitly classify genuine data/test obsolescence. Do NOT make tests pass by weakening assertions or changing expected results without proving which representation is authoritative.

**Core rule:** If the framework disagrees with reality, determine which representation is authoritative before changing anything.

---

## Failure Classification Results

### Class A: Genuine Framework Defects (FIXED)

| Failure | Root Cause | Fix Applied |
|---------|------------|-------------|
| `test_plan_help` | `ObligationKind` enum missing `INTEGRATION` and `E2E` values used in `control_plane.py` Literal types | Added `INTEGRATION = "integration"` and `E2E = "e2e"` to `ObligationKind` enum in `obligation.py:58-70` |
| `test_plan_backend_no_crash` | Same as above | Same fix |
| `test_plan_frontend_no_crash` | Same as above | Same fix |
| `test_plan_runtime_no_crash` | Same as above | Same fix |
| `test_inspect_evidence_cleanup_dry_run` | Missing CLI handler for `evidence-cleanup` in `control_plane_facade.py:inspect()` | Added `elif q == "evidence-cleanup"` branch routing to `cmd_evidence_cleanup()` in `control_plane_facade.py:516-526` |

### Class B: Architecture-Provider / Data Reconciliation (FIXED / CLASSIFIED)

| Failure | Root Cause | Resolution |
|---------|------------|------------|
| `test_router_change_yields_endpoints` | Accounts router has no endpoint edges in execution graph (data generation gap) | **CLASSIFIED AS INVENTORY_GAP** — Platform/non-engine routers not captured in architecture inventory. Requires inventory regeneration, not code fix. |
| `test_backend_mapper_change_propagates_to_frontend` | Chain map `mappers`/`viewModels` empty due to empty `engine.capabilities` | **FIXED** — Added capability→engine inference in `provider.py:_build_frontend()` (lines 517-530) |
| `test_backend_router_change_propagates_to_frontend` | Same root cause | **FIXED** — Same fix |
| `test_backend_bridge_impact_is_evidence_backed` | Same root cause | **FIXED** — Same fix |
| `test_frontend_selection_provenance_records_chain_map_source` | Same root cause | **FIXED** — Same fix |

### Class C: Contract Representation Mismatch (CLASSIFIED)

| Drift | Classification | Evidence |
|-------|----------------|----------|
| `/api/reconciliation/${id}/reject` | **NORMALIZATION_MISMATCH** | Backend has `POST /{reconciliation_id}/reject` with `/api/reconciliation` prefix; cross-layer-map stores relative paths |
| `/api/reconciliation/scan` | **NORMALIZATION_MISMATCH** | Backend has `GET /scan` with prefix; drift detection compares absolute vs relative |
| `/api/reconciliation/${id}/confirm` | **NORMALIZATION_MISMATCH** | Backend has `POST /{reconciliation_id}/confirm` with prefix |
| `/api/reconciliation` | **NORMALIZATION_MISMATCH** | Backend has `GET ` (empty) with prefix |
| `/api/reconciliation/pending` | **NORMALIZATION_MISMATCH** | Backend has `GET /pending` with prefix |
| `/platform/v1/architecture/authority/${encodeURIComponent(name!)}` | **NORMALIZATION_MISMATCH + INVENTORY_GAP** | Backend has `/platform/v1/architecture/authority/{name}`; platform router not engine-owned |
| `/platform/v1/verification/run/group` | **INVENTORY_GAP** | Platform router endpoint exists but not in architecture inventory |
| `/platform/v1/architecture/authorities` | **INVENTORY_GAP** | Platform router endpoint exists but not in architecture inventory |
| `/platform/v1/verification/run` | **INVENTORY_GAP** | Platform router endpoint exists but not in architecture inventory |
| `/platform/v1/capabilities/${encodeURIComponent(capabilityId!)}` | **NORMALIZATION_MISMATCH + INVENTORY_GAP** | Backend has `/platform/v1/capabilities/{capability_id}` |
| `/platform/v1/capabilities/${capabilityId}` | **NORMALIZATION_MISMATCH + INVENTORY_GAP** | Backend has `/platform/v1/capabilities/{capability_id}` |
| `/platform/v1/tasks/${taskId}/cancel` | **NORMALIZATION_MISMATCH + INVENTORY_GAP** | Backend has `/platform/v1/tasks/{task_id}/cancel` |
| `/platform/v1/verification/run/affected` | **INVENTORY_GAP** | Platform router endpoint exists but not in architecture inventory |
| `/platform/v1/verification/run/full` | **INVENTORY_GAP** | Platform router endpoint exists but not in architecture inventory |
| `/platform/v1/verification/runs/recent` | **INVENTORY_GAP** | Platform router endpoint exists but not in architecture inventory |
| `/platform/v1/capabilities` (×2) | **INVENTORY_GAP** | Platform router endpoint exists but not in architecture inventory |
| `/platform/v1/capabilities/${encodeURIComponent(capabilityId!)}/graph` | **NORMALIZATION_MISMATCH + INVENTORY_GAP** | Backend has `/platform/v1/capabilities/{capability_id}/graph` |
| `/platform/v1/health` | **INVENTORY_GAP** | Platform router endpoint exists but not in architecture inventory |
| `/platform/v1/events?limit=${limit}` | **NORMALIZATION_MISMATCH + INVENTORY_GAP** | Backend has `/platform/v1/events`; query param format differs |
| `/platform/v1/architecture/${kind}` | **NORMALIZATION_MISMATCH + INVENTORY_GAP** | Backend has multiple `/platform/v1/architecture/*` endpoints |

---

## Root-Cause Matrix (Required Output)

| Failure / Drift | Root Cause | Authority | Fix | Certification Impact |
|-----------------|-----------|-----------|-----|---------------------|
| `test_plan_help` (and 3 variants) | `ObligationKind` enum incomplete vs `control_plane.py` Literal usage | `control_plane.py` is authoritative (uses "integration", "e2e") | Added missing enum values | Framework defect FIXED |
| `test_inspect_evidence_cleanup_dry_run` | Missing CLI route in facade vs canonical doc | `canonical_control_plane.py` documents it; `evidence_retention.py` implements it | Added CLI handler in facade | Framework defect FIXED |
| `test_router_change_yields_endpoints` | Execution graph lacks endpoint→router edges for non-engine routers | Architecture inventory generation is authoritative | **CLASSIFIED** — inventory regeneration needed | Trust boundary: provider gap documented |
| `test_backend_mapper_change_*` (3 tests) | Chain map empty `mappers`/`viewModels` due to empty `engine.capabilities` | Provider is authoritative; data was missing | Added capability→engine inference in provider | Trust boundary: provider gap FIXED |
| `test_frontend_selection_provenance_*` | Same as above | Same | Same fix | Same |
| 5× reconciliation drifts | Cross-layer-map stores relative paths; frontend calls absolute with prefix | Backend router source is authoritative | **CLASSIFIED** — NORMALIZATION_MISMATCH | Contract truth: normalization issue documented |
| 16× platform drifts | Platform router not captured in architecture inventory (non-engine) | Backend router source is authoritative | **CLASSIFIED** — INVENTORY_GAP | Contract truth: inventory scope documented |
| BL-002 (already fixed in O-5-R2) | Same root cause as chain-map mappers bug — empty `engine.capabilities` | Provider now correctly links capabilities | Already documented; fix validated | Already documented |

---

## Changes Made

### Source Code Fixes

1. **`runtime/foundation/verification/obligation.py:58-70`** — Added `INTEGRATION = "integration"` and `E2E = "e2e"` to `ObligationKind` enum to match `control_plane.py` Literal types.

2. **`runtime/foundation/verification/control_plane_facade.py:516-526`** — Added `evidence-cleanup` CLI handler in `inspect()` method, routing to existing `cmd_evidence_cleanup()` implementation.

3. **`runtime/foundation/architecture/provider.py:517-530`** — Added capability→engine inference mapping in `_build_frontend()` to populate `cap_engines` from naming convention (`use{Name}Capability` → `{name}_engine`). This fixes the root cause of empty `engine.capabilities` which cascaded to empty chain map `capabilities`, `mappers`, and `viewModels`.

### Test Updates

4. **`runtime/tests/test_cross_layer_planner.py:508`** — Updated BL-002 test expectation from `affected_capabilities == []` to `affected_capabilities == ["useLoansCapability"]` because the provider now correctly links `loan_engine` to `useLoansCapability`. The test comment previously documented this as a "pre-existing architecture provider data gap" which is now resolved.

### Generated Artifacts (Auto-regenerated)

- `runtime/generated/frontend-backend-map.json` — Regenerated with updated chain map data
- `runtime/generated/symbol-cache.json` — Updated timestamps

---

## Validation Results

### O-5-R2 Certification Preserved (58/58 tests pass)
```
runtime/tests/test_o5_frontend_backend_scenarios.py     PASSED
runtime/tests/test_cross_layer_planner.py               PASSED
runtime/tests/test_blast_radius_integration.py          PASSED
runtime/tests/test_frontend_backend_sync_deep.py        PASSED
runtime/tests/test_symbol_resolution_edge_cases.py      PASSED
```

### CLI Contract Tests (7/7 pass)
```
test_plan_help                     PASSED
test_plan_backend_no_crash         PASSED
test_plan_frontend_no_crash        PASSED
test_plan_runtime_no_crash         PASSED
test_inspect_evidence_cleanup_dry_run  PASSED
... (2 other tests)                PASSED
```

### Engineering Intelligence Tests (34/35 pass)
```
test_router_change_yields_endpoints  FAILED (CLASSIFIED: INVENTORY_GAP)
All other 34 tests                  PASSED
```

### Regression Suite
```
evidence/scope/mutation tests       2 passed, 5 deselected
```

### Lint/Typecheck
- `obligation.py`: ✅ ruff clean, ✅ mypy clean
- `provider.py`: ✅ ruff clean, ✅ mypy clean
- `control_plane_facade.py`: Pre-existing ruff errors (unrelated to changes)
- `test_cross_layer_planner.py`: ✅ ruff clean

---

## Open Items (Requires Inventory Regeneration)

1. **Platform router endpoints** — The `backend/src/routers/platform.py` router (prefix `/platform/v1`) is not engine-owned and thus not captured in the architecture inventory. This affects 16 contract drifts and the `test_router_change_yields_endpoints` test. Resolution: Regenerate architecture inventory with platform router support.

2. **Endpoint normalization** — Cross-layer-map stores relative endpoint paths (e.g., `GET /scan`) while frontend calls absolute paths with router prefix (e.g., `/api/reconciliation/scan`). The drift detection should apply router prefixes during comparison. Resolution: Fix cross-layer-graph generation normalization logic.

---

## Verdict

**CERTIFIED** — All genuine framework defects fixed, all drifts classified with evidence, O-5-R2 certification evidence remains intact (58/58 tests pass). Working tree reconciled with only intended source changes and expected generated artifact updates.

The single remaining test failure (`test_router_change_yields_endpoints`) is correctly classified as an architecture inventory data gap, not a code defect. The test asserts behavior that requires inventory regeneration to capture non-engine (platform) routers.

---

## Implementation Notes

**Order followed:** C58-A (baseline lock) → C58-B (ObligationKind) → C58-C (evidence-cleanup) → C58-D/G (provider capability linking) → C58-E (classification) → C58-F (drift classification) → C58-H (regression) → C58-I (certification).

**Key insight:** The 5 engineering intelligence test failures + BL-002 shared a single root cause: `engine.capabilities` tuples were empty because the architecture discovery pipeline doesn't generate capability→engine edges, and the provider had no fallback inference. Adding the naming-convention inference in `provider.py:_build_frontend()` fixed all simultaneously.

**No destructive operations:** No files deleted, no test assertions weakened to match incomplete data, no mutation campaign run, no O-6 scope creep.